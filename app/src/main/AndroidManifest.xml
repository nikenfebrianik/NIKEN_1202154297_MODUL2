<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.showup.niken.niken_1202154297_modul2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        //menjadikan splash yang ditampilkan pertama dan menghilangkan toolbar
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DineInActivity" />
        <activity android:name=".TakeAwayActivity" />
        <activity
            android:name=".MenuDetail"
            android:label="@string/title_activity_daftar_menu"
            android:parentActivityName=".RecyclerViewActivity" />
        <activity android:name=".RecyclerViewActivity" />
    </application>

</manifest>